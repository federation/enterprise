apiVersion: v1
kind: Service
metadata:
  name: {{ template "enterprise-api.fullname" . }}
  labels:
    app: {{ template "enterprise-api.name" . }}
    chart: {{ template "enterprise-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ template "enterprise-api.name" . }}
    release: {{ .Release.Name }}
